  type Query {
    getUser(id: Int!): User
  }
  type Mutation {
    addUser(firstName: String!, lastName: String, age: Int!): Boolean!
    deleteUser(id : Int!) : Boolean!
  }

  type User {
    id: Int!
    firstName: String!
    lastName: String!
    age: Int!
  }